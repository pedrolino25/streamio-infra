{
  "name": "s3-dispatcher",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc",
    "zip": "npm run build && cp package.json dist/ && cd dist && npm install --production && zip -r s3-dispatcher.zip index.js package.json node_modules"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.540.0",
    "@aws-sdk/client-sqs": "^3.540.0",
    "@aws-sdk/client-ecs": "^3.540.0"
  },
  "devDependencies": {
    "@types/node": "^25.0.3",
    "typescript": "^5.3.0"
  }
}
